interface:
  display_name: "Meeting Reader HTML"
  short_description: "Create a polished full-detail HTML meeting brief"
  default_prompt: "Use $summary-meeting-html to turn this transcript into a readable, self-contained HTML meeting brief."
